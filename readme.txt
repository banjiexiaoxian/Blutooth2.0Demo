1、uuid的选取问题
Android设备可以自定义UUID，但是外部蓝牙串口设备必须统一按照蓝牙官方文档中定义的那个UUID
2、为什么socket必须使用反射获取？
因为Android版本>4.2的问题
3、蓝牙连接的实现方式。
要做好可能成为服务端的准备，需要开启一个监听客户端发起请求的线程；同时也要作为一个能发起连接请求的客户端。
然而服务端的监听、客户端请求的发起、连接的管理都需要运行在单独的线程中。因为监听方法会阻塞、数据流读写方法会阻塞，客户端发起的connect方法也会阻塞。
